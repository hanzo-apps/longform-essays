/**
 * The warm-paper editorial palette — the app's ONE place for colour. Longform is
 * set like a well-made book page: a cream sheet, warm near-black ink, footnote
 * gray for metadata, hairline rules, and a single muted rust accent used sparingly.
 */
export const paper = '#FBF7EF'     // the cream sheet
export const panel = '#F4EEDF'     // a slightly deeper cream (hover / inset)
export const ink = '#26221E'       // warm near-black — headlines + strong text
export const inkSoft = '#463F37'   // long-form body text, a touch softer than ink
export const footnote = '#9B9184'  // metadata gray (dates, reading time, labels)
export const rule = '#E6DECE'      // hairline rules on cream
export const accent = '#9A3B21'    // muted rust — links, active marks, quotes

/** Darker ink shades for button hover / press, kept beside the palette. */
export const inkHover = '#3A342D'
export const inkPress = '#14110E'
